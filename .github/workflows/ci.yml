name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  # Placeholder gate so branch protection has a required status check to wait on.
  # Replace with real backend/frontend jobs once the stack is decided.
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Status
        run: echo "CI skeleton — add lint/type/test/security steps when the stack lands."

  # ─────────────────────────────────────────────────────────────────────────
  # When we pick a Python backend, uncomment and adapt (modeled on sparkgoose):
  #
  # backend:
  #   runs-on: ubuntu-latest
  #   services:
  #     postgres:
  #       image: pgvector/pgvector:pg16
  #       env:
  #         POSTGRES_USER: helpvine
  #         POSTGRES_PASSWORD: helpvine
  #         POSTGRES_DB: helpvine_test
  #       ports: ["5432:5432"]
  #       options: >-
  #         --health-cmd pg_isready --health-interval 10s
  #         --health-timeout 5s --health-retries 5
  #   env:
  #     DATABASE_URL: postgresql+asyncpg://helpvine:helpvine@localhost:5432/helpvine_test
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.12"
  #         cache: "pip"
  #         cache-dependency-path: backend/requirements.txt
  #     - run: pip install -r backend/requirements.txt
  #     - run: ruff check backend/
  #     - run: mypy backend/app/ --strict --ignore-missing-imports
  #     - run: bandit -r backend/app/ -q
  #     - run: pip-audit -r backend/requirements.txt
  #     - run: pytest backend/tests/ -v --cov=backend/app --cov-report=term-missing --cov-fail-under=80
  #
  # ─────────────────────────────────────────────────────────────────────────
  # When we pick a Node/Next frontend, uncomment and adapt:
  #
  # frontend:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "22"
  #         cache: "npm"
  #         cache-dependency-path: frontend/package-lock.json
  #     - run: npm ci
  #       working-directory: frontend
  #     - run: npx eslint .
  #       working-directory: frontend
  #     - run: npx tsc --noEmit
  #       working-directory: frontend
  #     - run: npm audit --audit-level=high
  #       working-directory: frontend
