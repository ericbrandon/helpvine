# Notes

Drop documents here that aren't part of the codebase — design jottings,
meeting notes, scratch research, reference material.

These files are tracked in git. If a note contains anything sensitive
(keys, tokens, credentials), put it in `secrets/` instead — that folder is
gitignored.
